class Solution {
public:
    string reverseString(string s) {
        
        int l = s.size();
        string res(l, 0);
        for (int i=0; i<l; i++)
        {
            res[i] = s[l-1-i];
        }
        return res;
    }
};
